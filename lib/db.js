class Database {
  constructor(host, port, name) {
    this.host = host;
    this.port = port;
    this.name = name;
  }
  // connect() {}
  // create_user() {}
  // get_users(req, res)
  // get_user_by_id() {}
  // update_user() {}
  // delete_user_by_id() {}
}

module.exports = Database;
